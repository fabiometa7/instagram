# Rename to `.env` and fill in for real Meta Graph API insights.
# Without this file the app falls back to public scraping (still works,
# just no reach/impressions/watch-time from the source).

IG_GRAPH_TOKEN=EAA...long-lived-token
IG_BUSINESS_ID=17841400000000000
